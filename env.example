# Required: clients send this as X-Api-Key, Authorization: Bearer, or ?apikey=
API_KEY=change-me

# Password of the encrypted .portfolio file
PORTFOLIO_PASSWORD=your-portfolio-password

# Google Drive file (id from the share URL, or the full URL)
PORTFOLIO_GOOGLE_DRIVE_FILE_ID=1abc...xyz


# Service account: either the JSON contents or a path to the key file
GOOGLE_SERVICE_ACCOUNT_JSON=
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Cache lifetime in minutes (default 15). Pass ?nocache to force a refresh.
CACHE_TTL_MINUTES=15

# Optional local file (used when Drive is not configured, e.g. tests)
# PORTFOLIO_FILE=./tmp/portfolio.portfolio

# PORT=9292
# API_KEY_HEADER=X-Api-Key
# INCLUDE_RETIRED=true

# Fineco XLS/XLSX import: skip preamble rows before the table (header is on row 13)
IMPORT_FINECO_XLS_SKIP_LINES=13

# Rows written per Sheets API request (avoid 429 write-quota errors)
SYNC_GOOGLE_DRIVE_ROWS_CHUNK=1000
# Rows shown at once in the sync preview lists
SYNC_GOOGLE_DRIVE_PREVIEW_ROWS=20
# Google Spreadsheet used by bin/utils/sync.rb (share Editor with the service account)
SYNC_GOOGLE_DRIVE_FILE_ID=1xyz...abc
# Spreadsheet used by live sync tests (share Editor with the service account)
SYNC_GOOGLE_DRIVE_FILE_ID_TEST=1test...abc
# Rows to leave untouched at the top of each sheet (header is written after these)
SYNC_GOOGLE_DRIVE_FILE_SKIP_ROWS=1

